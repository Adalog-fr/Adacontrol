with Text_Io, Direct_Io;             -- Ada.Direct_IO
with Ada.Text_Io.Text_Streams;       -- Ada.Text_Io.Text_Streams
with Sequential_IO;                  -- Ada.Sequential_IO
with Ada.Strings;
procedure T_Other_Dependencies is
begin
   null;
end T_Other_Dependencies;
