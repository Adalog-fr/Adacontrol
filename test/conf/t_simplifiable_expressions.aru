               count  simplifiable_expressions;
Range_Or_True: check  simplifiable_expressions (Range, Logical_True);
False:         search simplifiable_expressions (Logical_False);
               check  simplifiable_expressions (parentheses);
               search simplifiable_expressions (conversion);
               search simplifiable_expressions (Logical_not);
               check  simplifiable_expressions (Logical_Redundant);
               check  simplifiable_expressions (Membership);
               search simplifiable_expressions (if_not);
