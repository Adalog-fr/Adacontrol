includes_blocks: check reduceable_scope (variable, subprogram, type, package);
except_blocks:   check reduceable_scope (no_blocks generic);
use_clauses:     check reduceable_scope (use, use_type, use_all_type);
to_body:         check reduceable_scope (to_body exception);
