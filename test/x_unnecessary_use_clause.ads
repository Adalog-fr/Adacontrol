with Text_IO, Ada.Strings.Unbounded, Ada.Numerics;
use Text_IO, Ada.Strings.Unbounded, Ada.numerics; -- use for Text_IO can be moved to body, Numerics not used
procedure X_Unnecessary_Use_Clause (S : Unbounded_String);
