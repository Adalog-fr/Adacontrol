Reserved_2005: check naming_convention (all, not "interface", 
                                             not "overriding",
                                             not "synchronized");
Reserved_2012: check naming_convention (all, not "some");